#actual clique finder
import networkx as nx


def actual_clique_finder(n,k,l, pmk, pml): #takes in two lists where i should find the possible k-clique and l-inde set
    #make eve  for adj_matrix in pmk:
        
    
  
    #find a path of length (nodes -1) 
    

#in the set of pmk find atleast 1 clique
    #step 1 - start by finding cycles of length k
    #step 2 - ensure that 
    
    #in the set of pml, find atleast 1 indep set
    
    return